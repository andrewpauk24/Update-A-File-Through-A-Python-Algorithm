# Update-A-File-Through-A-Python-Algorithm
In this project, I developed a Python algorithm to automate the process of updating an IP address allow list used to control access to restricted organizational content. The system relies on an "allow_list.txt" file that stores approved IP addresses, while a separate remove list identifies addresses that should no longer have access.
